import { FormEvent, useEffect, useMemo, useRef, useState } from "react";
import { Archive, Bot, Check, ChevronDown, Copy, Edit3, Globe2, Languages, LoaderCircle, Menu, MessageSquare, MoreHorizontal, PanelLeftClose, Plus, Search, Settings2, SquarePen, Trash2, X } from "lucide-react";
import { api, getSession, setSession } from "./api";
import { useAppStore } from "./store";
import type { Conversation, DictionaryResponse, Message, Provider, Session } from "./types";

const now = () => new Date().toISOString();
const uuid = () => crypto.randomUUID();

export function App() {
  const { session, setSession: saveSession } = useAppStore();
  useEffect(() => { const raw = localStorage.getItem("malim-session"); if (raw) { const saved = JSON.parse(raw) as Session; setSession(saved); saveSession(saved); } }, [saveSession]);
  if (!session) return <Auth onSession={(value) => { localStorage.setItem("malim-session", JSON.stringify(value)); setSession(value); saveSession(value); }} />;
  return <Chat />;
}

function Auth({ onSession }: { onSession: (session: Session) => void }) {
  const [mode, setMode] = useState<"login" | "signup">("login"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setError(null); try { onSession(mode === "login" ? await api.login(email, password) : await api.signup(email, password, name)); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to continue."); } finally { setBusy(false); } }
  return <main className="auth-shell"><section className="auth-brand"><div className="brand-mark"><Bot size={28} /></div><h1>malim_chat</h1><p>One secure place for the AI models your work depends on.</p></section><form className="auth-form" onSubmit={submit}><h2>{mode === "login" ? "Welcome back" : "Create your account"}</h2>{mode === "signup" && <label>Display name<input value={name} required maxLength={80} onChange={(e) => setName(e.target.value)} /></label>}<label>Email<input type="email" value={email} required autoComplete="email" onChange={(e) => setEmail(e.target.value)} /></label><label>Password<input type="password" value={password} minLength={12} required autoComplete={mode === "login" ? "current-password" : "new-password"} onChange={(e) => setPassword(e.target.value)} /></label>{error && <p className="form-error">{error}</p>}<button className="primary" disabled={busy}>{busy ? <LoaderCircle className="spin" size={17} /> : null}{mode === "login" ? "Sign in" : "Create account"}</button><button type="button" className="text-button" onClick={() => { setMode(mode === "login" ? "signup" : "login"); setError(null); }}>{mode === "login" ? "Need an account? Sign up" : "Already have an account? Sign in"}</button></form></main>;
}

function Chat() {
  const state = useAppStore(); const { conversations, providers, activeId, messages, sidebarOpen, busy, error } = state; const [providerOpen, setProviderOpen] = useState(false); const [searchEnabled, setSearchEnabled] = useState(false); const [lookupWord, setLookupWord] = useState<string | null>(null);
  useEffect(() => { void bootstrap(); }, []);
  useEffect(() => { if (activeId && !messages[activeId]) void loadMessages(activeId); }, [activeId]);
  async function bootstrap() { try { const [chats, configured] = await Promise.all([api.conversations(), api.providers()]); state.setConversations(chats.items); state.setProviders(configured); if (chats.items[0]) state.setActiveId(chats.items[0].id); } catch (cause) { state.setError(cause instanceof Error ? cause.message : "Unable to load your workspace."); } }
  async function loadMessages(id: string) { try { const page = await api.messages(id); state.setMessages(id, page.items); } catch (cause) { state.setError(cause instanceof Error ? cause.message : "Unable to load messages."); } }
  async function newChat() { try { const conversation = await api.createConversation({ provider_id: providers[0]?.id, model: providers[0]?.default_model }); state.setConversations([conversation, ...conversations]); state.setActiveId(conversation.id); state.setMessages(conversation.id, []); } catch (cause) { state.setError(cause instanceof Error ? cause.message : "Unable to create a conversation."); } }
  async function archive(id: string) { state.setConversations(conversations.filter((item) => item.id !== id)); if (activeId === id) state.setActiveId(conversations.find((item) => item.id !== id)?.id ?? null); try { await api.updateConversation(id, { archived: true }); } catch (cause) { await bootstrap(); state.setError(cause instanceof Error ? cause.message : "Unable to archive conversation."); } }
  function signOut() { localStorage.removeItem("malim-session"); setSession(null); state.setSession(null); }
  return <main className="app-shell"><Sidebar conversations={conversations} activeId={activeId} open={sidebarOpen} onNew={newChat} onSelect={state.setActiveId} onArchive={archive} onSettings={() => setProviderOpen(true)} onClose={() => state.setSidebarOpen(false)} userName={state.session?.user.display_name ?? "Account"} onSignOut={signOut} /><section className="chat-shell"><header className="chat-header"><button className="icon-button mobile-only" aria-label="Open navigation" onClick={() => state.setSidebarOpen(true)}><Menu size={20} /></button><div className="header-title"><span>{activeId ? conversations.find((item) => item.id === activeId)?.title ?? "Conversation" : "malim_chat"}</span><ChevronDown size={16} /></div><div className="header-actions"><button className={`tool-button ${searchEnabled ? "active" : ""}`} onClick={() => setSearchEnabled(!searchEnabled)} title="Online search"><Globe2 size={17} /><span>Search</span></button><button className="icon-button" aria-label="Configure providers" onClick={() => setProviderOpen(true)}><Settings2 size={19} /></button></div></header>{error && <div className="notice"><span>{error}</span><button className="icon-button" aria-label="Dismiss" onClick={() => state.setError(null)}><X size={16} /></button></div>}<ConversationView conversation={activeId ? conversations.find((item) => item.id === activeId) ?? null : null} messages={activeId ? messages[activeId] ?? [] : []} providers={providers} searchEnabled={searchEnabled} busy={busy} onSend={async (content) => { if (!activeId) return; await sendMessage(activeId, content, searchEnabled); }} onEdit={editMessage} onDelete={deleteMessage} onLookup={setLookupWord} onCompact={async () => { if (activeId) { try { await api.compact(activeId); state.setError("Conversation context was compacted."); } catch (cause) { state.setError(cause instanceof Error ? cause.message : "Compaction failed."); } } }} /> </section>{providerOpen && <ProviderDialog providers={providers} onClose={() => setProviderOpen(false)} onChanged={async () => { state.setProviders(await api.providers()); }} />}{lookupWord && <DictionaryDialog word={lookupWord} onClose={() => setLookupWord(null)} />}</main>;
  async function sendMessage(conversationId: string, content: string, search: boolean) { const mutationId = uuid(); const optimistic: Message = { id: `local-${mutationId}`, conversation_id: conversationId, sequence: Number.MAX_SAFE_INTEGER - Date.now(), client_mutation_id: mutationId, role: "user", content, content_format: "markdown", status: "pending", model: null, token_count: 0, search_sources: [], edited_at: null, created_at: now(), updated_at: now(), optimistic: true }; state.upsertMessage(conversationId, optimistic); state.setBusy(true); try { const sent = await api.createMessage(conversationId, content, mutationId, search); state.upsertMessage(conversationId, sent); const pendingId = `assistant-${mutationId}`; state.upsertMessage(conversationId, { ...optimistic, id: pendingId, sequence: sent.sequence + 0.5, client_mutation_id: null, role: "assistant", content: "", status: "streaming" }); const answer = await api.respond(conversationId, sent.id, search); state.removeMessage(conversationId, pendingId); state.upsertMessage(conversationId, answer); const updated = await api.conversations(); state.setConversations(updated.items); } catch (cause) { state.removeMessage(conversationId, optimistic.id); state.setError(cause instanceof Error ? cause.message : "Message delivery failed."); } finally { state.setBusy(false); } }
  async function editMessage(message: Message, content: string) { if (!activeId) return; const before = messages[activeId] ?? []; state.upsertMessage(activeId, { ...message, content }); try { state.upsertMessage(activeId, await api.updateMessage(activeId, message.id, content)); } catch (cause) { state.setMessages(activeId, before); state.setError(cause instanceof Error ? cause.message : "Could not edit message."); } }
  async function deleteMessage(message: Message) { if (!activeId) return; const before = messages[activeId] ?? []; state.removeMessage(activeId, message.id); try { await api.deleteMessage(activeId, message.id); } catch (cause) { state.setMessages(activeId, before); state.setError(cause instanceof Error ? cause.message : "Could not delete message."); } }
}

function Sidebar({ conversations, activeId, open, onNew, onSelect, onArchive, onSettings, onClose, userName, onSignOut }: { conversations: Conversation[]; activeId: string | null; open: boolean; onNew: () => void; onSelect: (id: string) => void; onArchive: (id: string) => void; onSettings: () => void; onClose: () => void; userName: string; onSignOut: () => void }) { return <aside className={`sidebar ${open ? "open" : ""}`}><div className="sidebar-top"><div className="wordmark"><Bot size={20} /><span>malim_chat</span></div><button className="icon-button desktop-only" aria-label="Close navigation"><PanelLeftClose size={19} /></button><button className="icon-button mobile-only" aria-label="Close navigation" onClick={onClose}><X size={19} /></button></div><button className="new-chat" onClick={onNew}><SquarePen size={17} />New chat</button><nav aria-label="Conversation history" className="history"><p>Conversations</p>{conversations.map((conversation) => <div className={`conversation-row ${conversation.id === activeId ? "selected" : ""}`} key={conversation.id}><button onClick={() => onSelect(conversation.id)}><MessageSquare size={16} /><span>{conversation.title}</span></button><button className="row-action" aria-label={`Archive ${conversation.title}`} onClick={() => onArchive(conversation.id)}><Archive size={15} /></button></div>)}</nav><div className="sidebar-bottom"><button className="settings-button" onClick={onSettings}><Settings2 size={17} />Providers & settings</button><div className="account-row"><div className="avatar">{userName.slice(0, 1).toUpperCase()}</div><span>{userName}</span><button className="row-action" aria-label="Sign out" onClick={onSignOut}><MoreHorizontal size={17} /></button></div></div></aside>; }

function ConversationView({ conversation, messages, providers, searchEnabled, busy, onSend, onEdit, onDelete, onLookup, onCompact }: { conversation: Conversation | null; messages: Message[]; providers: Provider[]; searchEnabled: boolean; busy: boolean; onSend: (content: string) => Promise<void>; onEdit: (message: Message, content: string) => Promise<void>; onDelete: (message: Message) => Promise<void>; onLookup: (word: string) => void; onCompact: () => Promise<void> }) { const [input, setInput] = useState(""); const endRef = useRef<HTMLDivElement>(null); useEffect(() => endRef.current?.scrollIntoView({ behavior: "smooth" }), [messages.length, busy]); const usage = conversation ? Math.min(100, Math.round((conversation.context_tokens / conversation.context_window) * 100)) : 0; async function submit(event: FormEvent) { event.preventDefault(); const text = input.trim(); if (!text || busy) return; setInput(""); await onSend(text); } if (!conversation) return <section className="empty-state"><div className="empty-logo"><Bot size={34} /></div><h1>How can I help you today?</h1><p>Start a new conversation to work with your configured AI providers.</p></section>; return <><section className="message-list">{messages.map((message) => <MessageBubble key={message.id} message={message} onEdit={onEdit} onDelete={onDelete} onLookup={onLookup} />)}<div ref={endRef} /></section><footer className="composer-wrap"><div className="context-meter"><span>Context {conversation.context_tokens.toLocaleString()} / {conversation.context_window.toLocaleString()} tokens</span><div><i style={{ width: `${usage}%` }} /></div><button onClick={() => void onCompact()} title="Compact previous context">Compact</button></div><form className="composer" onSubmit={submit}><textarea value={input} onChange={(event) => setInput(event.target.value)} placeholder={providers.length ? "Message malim_chat" : "Add a provider in settings to begin"} disabled={!providers.length || busy} rows={1} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); void submit(event); } }} /><div className="composer-controls"><span className={searchEnabled ? "search-chip enabled" : "search-chip"}><Search size={14} />{searchEnabled ? "Web search on" : "Web search off"}</span><button className="send-button" aria-label="Send message" disabled={!input.trim() || busy || !providers.length}>{busy ? <LoaderCircle className="spin" size={18} /> : <Check size={18} />}</button></div></form><p className="disclaimer">AI responses may be inaccurate. Review important information.</p></footer></>; }

function MessageBubble({ message, onEdit, onDelete, onLookup }: { message: Message; onEdit: (message: Message, content: string) => Promise<void>; onDelete: (message: Message) => Promise<void>; onLookup: (word: string) => void }) { const [editing, setEditing] = useState(false); const [draft, setDraft] = useState(message.content); const [selection, setSelection] = useState(""); function captureSelection() { const selected = window.getSelection()?.toString().trim() ?? ""; setSelection(selected.length <= 120 ? selected : ""); } return <article className={`message ${message.role === "user" ? "user" : "assistant"}`}><div className="message-avatar">{message.role === "user" ? "You" : <Bot size={17} />}</div><div className="message-body" onMouseUp={captureSelection} onTouchEnd={captureSelection}>{editing ? <div className="edit-box"><textarea value={draft} onChange={(event) => setDraft(event.target.value)} /><button className="primary small" onClick={() => { void onEdit(message, draft); setEditing(false); }}>Save</button><button className="text-button small" onClick={() => { setDraft(message.content); setEditing(false); }}>Cancel</button></div> : message.status === "streaming" ? <span className="typing"><i /><i /><i /></span> : <div className="message-content">{message.content}</div>}{message.search_sources?.length > 0 && <div className="sources">{message.search_sources.slice(0, 3).map((source) => <a href={source.url} target="_blank" rel="noreferrer" key={source.url}>{source.title}</a>)}</div>}<div className="message-tools"><button title="Copy" onClick={() => void navigator.clipboard.writeText(message.content)}><Copy size={14} /></button>{selection && <button title="Look up selected text" onClick={() => onLookup(selection)}><Languages size={14} /></button>}{message.role === "user" && !message.optimistic && <button title="Edit" onClick={() => setEditing(true)}><Edit3 size={14} /></button>}<button title="Delete" onClick={() => void onDelete(message)}><Trash2 size={14} /></button></div></div></article>; }

function ProviderDialog({ providers, onClose, onChanged }: { providers: Provider[]; onClose: () => void; onChanged: () => Promise<void> }) { const [name, setName] = useState(""); const [kind, setKind] = useState("openai_compatible"); const [baseUrl, setBaseUrl] = useState("https://api.openai.com"); const [key, setKey] = useState(""); const [model, setModel] = useState(""); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setError(null); try { await api.createProvider({ name, kind, base_url: baseUrl, api_key: key, default_model: model }); await onChanged(); setName(""); setKey(""); setModel(""); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to save provider."); } finally { setBusy(false); } } return <div className="modal-backdrop" role="presentation"><section className="modal" role="dialog" aria-modal="true" aria-labelledby="provider-title"><header><div><h2 id="provider-title">Providers</h2><p>Credentials are encrypted on the server and never returned to this device.</p></div><button className="icon-button" aria-label="Close" onClick={onClose}><X size={20} /></button></header><div className="provider-list">{providers.length === 0 ? <p className="muted">No providers configured.</p> : providers.map((provider) => <div className="provider-card" key={provider.id}><div><strong>{provider.name}</strong><span>{provider.default_model} · {provider.kind === "anthropic" ? "Anthropic" : "OpenAI-compatible"}</span></div><button className="icon-button danger" aria-label={`Delete ${provider.name}`} onClick={async () => { await api.deleteProvider(provider.id); await onChanged(); }}><Trash2 size={17} /></button></div>)}</div><form className="provider-form" onSubmit={submit}><h3>Add provider</h3><label>Name<input required value={name} onChange={(event) => setName(event.target.value)} placeholder="OpenAI, DeepSeek, Qwen..." /></label><label>API format<select value={kind} onChange={(event) => setKind(event.target.value)}><option value="openai_compatible">OpenAI-compatible</option><option value="anthropic">Anthropic Messages API</option></select></label><label>Base URL<input type="url" required value={baseUrl} onChange={(event) => setBaseUrl(event.target.value)} /></label><label>API key<input type="password" required value={key} onChange={(event) => setKey(event.target.value)} /></label><label>Default model<input required value={model} onChange={(event) => setModel(event.target.value)} placeholder="gpt-4o-mini" /></label>{error && <p className="form-error">{error}</p>}<button className="primary" disabled={busy}>{busy && <LoaderCircle className="spin" size={17} />}Save provider</button></form></section></div>; }

function DictionaryDialog({ word, onClose }: { word: string; onClose: () => void }) { const [dictionary, setDictionary] = useState<"russian_en" | "german_en" | "english_zh">(/[\u0400-\u04FF]/.test(word) ? "russian_en" : "english_zh"); const [data, setData] = useState<DictionaryResponse | null>(null); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false); useEffect(() => { let live = true; setBusy(true); setError(null); setData(null); void api.dictionary(word, dictionary).then((response) => { if (live) setData(response); }).catch((cause) => { if (live) setError(cause instanceof Error ? cause.message : "Dictionary lookup failed."); }).finally(() => { if (live) setBusy(false); }); return () => { live = false; }; }, [word, dictionary]); return <div className="modal-backdrop" role="presentation"><section className="modal dictionary-modal" role="dialog" aria-modal="true" aria-labelledby="dictionary-title"><header><div><h2 id="dictionary-title">{word}</h2><p>Local dictionary lookup</p></div><button className="icon-button" aria-label="Close" onClick={onClose}><X size={20} /></button></header><div className="dictionary-tabs"><button className={dictionary === "english_zh" ? "active" : ""} onClick={() => setDictionary("english_zh")}>English - Chinese</button><button className={dictionary === "german_en" ? "active" : ""} onClick={() => setDictionary("german_en")}>German - English</button><button className={dictionary === "russian_en" ? "active" : ""} onClick={() => setDictionary("russian_en")}>Russian - English</button></div>{busy && <p className="dictionary-loading"><LoaderCircle className="spin" size={17} />Looking up local dictionary...</p>}{error && <p className="form-error">{error}</p>}{data && data.entries.length === 0 && <p className="muted">No local entry found for this selection.</p>}<div className="dictionary-results">{data?.entries.map((entry, index) => <article className="dictionary-entry" key={`${entry.headword}-${index}`}><div className="dictionary-head"><h3>{entry.headword}</h3>{entry.pronunciation && <span>/{entry.pronunciation}/</span>}</div>{entry.translations.length > 0 && <section><h4>Chinese</h4><ul>{entry.translations.map((value) => <li key={value}>{value}</li>)}</ul></section>}{entry.definitions.length > 0 && <section><h4>Definitions</h4><ul>{entry.definitions.map((value) => <li key={value}>{value}</li>)}</ul></section>}{entry.forms.length > 0 && <section><h4>Forms</h4><div className="dictionary-pills">{entry.forms.map((value) => <span key={value}>{value}</span>)}</div></section>}{entry.examples.length > 0 && <section><h4>Examples</h4><ul>{entry.examples.map((value) => <li key={value}>{value}</li>)}</ul></section>}{entry.detail !== null && <details className="dictionary-detail"><summary>Entry detail</summary><pre>{typeof entry.detail === "string" ? entry.detail : JSON.stringify(entry.detail, null, 2)}</pre></details>}{entry.labels.length > 0 && <div className="dictionary-pills muted-pills">{entry.labels.map((value) => <span key={value}>{value}</span>)}</div>}</article>)}</div></section></div>; }
